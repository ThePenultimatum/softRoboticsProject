#include "pressureSensor.h"

void enterMeasurementState(int sensor, int airOrVacState) {
	// switch off the valve in parallel with the robot and then put the 
	//   valves in the appropriate configuration for vac or air pressure
	//   sensing
}
int takeMeasurement(int sensor) {
	return 1;
}